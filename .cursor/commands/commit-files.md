# Commit files

Split and commit current working tree changes using the `commit-manager` skill.

**This command delegates entirely to the `commit-manager` skill.** Load and execute `.cursor/skills/commit-manager/SKILL.md`.
