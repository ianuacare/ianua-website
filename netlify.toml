[build]
  command = "npm run build"
  publish = "dist"

# SPA: tutte le route servono index.html (utile se aggiungi routing client-side)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
